# Install PyTorch separately, matching your CUDA version, before installing
# the rest of this file -- see README.md for details.
# Example (CUDA 12.1):
#   pip install torch==2.1.2 torchvision==0.16.2 --index-url https://download.pytorch.org/whl/cu121

# Core scientific stack
numpy>=1.24,<2.0
Pillow>=10.0

# Hugging Face stack (LLaVA-v1.5 pins HF transformers <= 4.37.2)
transformers==4.37.2
tokenizers>=0.15,<0.16
accelerate>=0.27,<0.30
sentencepiece>=0.1.99
safetensors>=0.4.0
huggingface-hub>=0.20,<0.24

# Multimodal helpers used by the LLaVA stack
einops>=0.7.0
timm>=0.9.10
shortuuid>=1.0.11

# CLIP / vision encoders pulled in by LLaVA
open_clip_torch>=2.20.0

# Optional but useful for reproducibility / plotting
matplotlib>=3.7
pandas>=2.0
scikit-learn>=1.3
tqdm>=4.65

# LLaVA itself is installed separately from its upstream repository:
#   git clone https://github.com/haotian-liu/LLaVA.git && cd LLaVA && pip install -e .
# (or set LLAVA_HOME to an existing checkout).
